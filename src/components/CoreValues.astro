---
import '../styles/style.css'
import { Icon } from 'astro-icon/components'

const valueData = await fetch("https://raw.githubusercontent.com/julievictoria98/frontend-design/main/resources/json/corevalues.json").then((res) => res.json());
---

<article class="full">
    <div class="core_text_flex">
        <div class="text">
            <h1>Growing with our clints</h1>
            <h2>{valueData.title}</h2>
            <p>{valueData.subtitle}</p>
        </div>
        <button>Get in Touch</button>
        <ul class="card">
            {valueData.values.map((value) => (
                
                <li class="value_card_flex">
                <div class="icon">
                    <Icon name="layers"/>
               </div>
                <h3 class="bold">{value.title}</h3>
                <p>{value.description}</p>
                <button>Read more</button>
                </li>
            ))}
            
        </ul>
    </div>
</article>

<style>
    @layer components{
    article{
          --bg-color: var(--primary-color-3);
      & *:not(ul):not(ul *) {
        color: var(--secondary-color-2);
}
        }


    
    ul{
        display:flex;
        flex-wrap: wrap;
        gap:2.25rem;
        align-items: flex-start;
        padding: 0;

        

    }

    ul > li {

     background-color: var(--secondary-color-2); 
    border-radius: 1rem;
    padding: 1.2rem; 
    box-sizing: border-box;
     max-width: 15rem; */
     align-self: flex-start
     
}

    .core_text_flex{
        display: flex;
        flex-direction: column;
        gap: 2rem;
        
            /* align-items: flex-start; Align items to the start to avoid uniform height */
            
    }
    p{
        /* color: var(--primary-color-3); */
    }

    button{
        /* color:var(--primary-color-3); */
    }

    .icon{
        width:3rem;
    }

    .value_card_flex{
        display: flex;
        flex-direction: column;
        max-width: 20rem;
        gap: 1rem;
        padding: 2rem;
        align-items: flex-start;
    }

    }
</style>